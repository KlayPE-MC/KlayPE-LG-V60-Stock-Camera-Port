# Audio
audio.sys.mute.latency.factor=2
audio.offload.buffer.size.kb=32
audio.offload.gapless.enabled=true

# Camera
# Keep LGCamera out of the auxiliary allow-list. It expects getCameraIdList()
# to expose only public IDs 0/1 and accesses its logical IDs explicitly.
# Exposing every auxiliary ID makes it derive and query nonexistent ID 10.
vendor.camera.aux.packagelist=org.codeaurora.snapcam,com.android.camera

# CNE
persist.vendor.cne.feature=1

# Display
persist.demo.hdmirotationlock=false

# Fastbootd
ro.fastbootd.available=true

# GPS
persist.backup.ntpServer=0.pool.ntp.org

# IMS
persist.dbg.volte_avail_ovr=1
persist.dbg.vt_avail_ovr=1
persist.dbg.wfc_avail_ovr=1

# Media
media.settings.xml=/vendor/etc/media_profiles_vendor.xml
ro.media.recorder-max-base-layer-fps=60

# Netmgr
persist.data.df.dev_name=rmnet_usb0
persist.data.wda.enable=true
persist.rmnet.data.enable=true

# NFC
ro.nfc.port=I2C

# QC framework value-adds
ro.vendor.qti.va_aosp.support=1

# Time
persist.timed.enable=true
